# Published Configs

This directory is reserved for curated configs selected for reproduced or paper-facing results.

Ordinary experiment runs write selected tuning outputs under `runs/<experiment_name>/tuned/` and must not overwrite files here by default.
