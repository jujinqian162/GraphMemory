# HotpotQA dev-full preset

`configs/experiment/hotpotqa_dev_full.yaml` is the retained root preset for using the complete HotpotQA dev window as test data. Invoke it with `--config-name=hotpotqa_dev_full` and provide a run name. The filename preserves the corrected active YAML identifier; old JSON configuration names are unsupported.
